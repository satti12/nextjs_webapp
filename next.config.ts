import { image } from "framer-motion/client";
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["images.unsplash.com", "marketplace.canva.com"],
  },
};

export default nextConfig;
